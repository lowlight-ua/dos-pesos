---
system:
  certificateFilePath: "{{ greengrass.install.rootpath }}/device.pem.crt"
  privateKeyPath: "{{ greengrass.install.rootpath }}/private.pem.key"
  rootCaPath: "{{ greengrass.install.rootpath }}/AmazonRootCA1.pem"
  rootpath: "{{ greengrass.install.rootpath }}"
  thingName: "{{ greengrass.core.iot_thing_name }}"
services:
  aws.greengrass.Nucleus:
    componentType: "NUCLEUS"
    version: "{{ greengrass.install.version }}"
    configuration:
      awsRegion: "{{ aws.region }}"
      iotRoleAlias: "GreengrassCoreTokenExchangeRoleAlias"
      iotDataEndpoint: "{{ iotDataEndpoint }}"
      iotCredEndpoint: "{{ iotCredEndpoint }}"
